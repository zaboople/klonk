package org.tmotte.klonk.config;

/**
 * On the macintosh the leftmost menu gets the name of the boot class, so I made this
 * as the bootable mac class.
 */
public class Klonk {
  public static void main(final String[] args) {
    BootContext.main(args);
  }
}

